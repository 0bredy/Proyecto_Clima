# Copie este archivo como .env y ajuste los valores.
# El archivo .env NO se sube al repositorio (esta en .gitignore).

URL_API=https://api.open-meteo.com/v1/forecast
CIUDAD_POR_DEFECTO=panama
NOMBRE_BD=clima.db
